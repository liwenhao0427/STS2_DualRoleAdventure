# LocalMultiControl 功能效果实现记录（面向 VibeCoding 作者）

> 统计范围：当前仓库已有全部提交（截至 2026-03-15 21:22:09 +08:00）  
> 记录原则：按“日期 + 时间段”汇总重要成果；同一功能在同一时间段内反复调优仅记录一次。

## 2026-03-12（项目启动与核心链路打通）

### 08:38-10:09｜工程初始化与入口建立
- 初始化 `LocalMultiControl` 模组工程骨架，建立可迭代开发基础。
- 明确本地多控需求、阶段任务和可玩检查点，形成开发约束。
- 新增自协作入口（self-coop menu entry），把模式入口接到主流程。

### 12:12-15:59｜双人本地回环与控制权流转
- 启动本地回环双人大厅（loopback two-player lobby）。
- 增加角色切换热键与地图跟随投票能力，形成基础多控交互。
- 完成 ownership/control-switch 主干流程，实现“谁在控制、控制如何切换”的核心机制。

### 16:31-21:26｜稳定性迭代（iter-06/07）
- 修复并稳定：选角视觉同步、输入恢复、大厅高亮残留、回合切换、事件流程阻塞、结束回合卡死等关键问题。
- 修复奖励归属与敌方回合衔接，降低战斗阶段状态错位风险。
- 切人时重建遗物/药水 UI，改善多角色视图一致性。
- 兼容多人存档加载中的本地自协作恢复流程，降低读档异常。
- 调整为共享遗物并抑制本地加载等待弹窗，优化本地多控体验。

## 2026-03-13（分支行为与产物流程完善）

### 10:00-11:07｜模式行为与构建产物对齐
- 引入并落地 DualRoleAdventure 分支行为（本地多控模式基础行为）。
- 构建产物改为项目根输出 `DualRoleAdventure.dll`，并打通 `pck + dll` 联合部署路径。

### 22:41｜流程稳态与架构说明
- 继续稳固本地多控流程，并补充架构说明文档，便于后续扩展与回溯。

## 2026-03-14（玩法流程修复 + UI交互成型）

### 09:32-13:59｜奖励/商店/事件核心链路修复
- 修复商店随机池与归属相关问题，恢复关键显示逻辑。
- 修复卡牌奖励重复、宝箱/事件卡死、药水栏阻塞等主流程问题。
- 修复本地双人选择后动作恢复、下一幕双人就绪、共享事件自动代投等一致性问题。

### 15:31-16:06｜资源同步与入口形态升级
- 新增金币双角色同步与 `R` 切换热键，完善多角色资源一致性。
- 联机入口改为四卡并列样式，并修复间距与悬停联动问题，提升入口可用性。

### 20:34-22:03｜2~4人热调与上下文稳定
- 支持本地 2~4 角色、重复选角即时生效与重提交。
- 修复切人选角上下文错位、本地多控入口崩溃、人数扩容不生效、容量锁死等关键故障。
- 规避随机角色开局卡死，提升开局稳定性。

### 23:05-23:56｜实体切换按钮全链路接入
- 新增实体切换按钮，并接入角色选择页、头像区、战斗 UI。
- 修复战斗切人补丁初始化失败、无效补丁导致无法开局等阻断问题。
- 对按钮尺寸、图标、位置、对齐进行统一收敛，形成可用的交互样式。

## 2026-03-15（战斗控制细化与多人上限调优）

### 00:19-07:36｜切换控件持续收敛 + 金币镜像抑制
- 继续收敛选人页/战斗页切换按钮与加减按钮布局，减少遮挡和误触。
- 在遗物获得链路中抑制金币镜像广播，并前置保护，降低重复同步风险。

### 09:20-10:44｜瓦库接管与自动结束回合机制
- 增加瓦库遗物接管能力与“全员无牌自动结束回合”机制。
- 增加看门狗与左上列表勾选联动，补齐后台自动结束回合逻辑。
- 修复跨战斗状态残留（自动结束回合状态重置）。

### 11:21-12:13｜本地队伍上限与大厅布局调优
- 将本地多角色上限扩展到 16，并补齐网络位宽支持。
- 后续根据稳定性与布局约束收敛到 12，上线前固定全瓦库位置并优化大厅列对齐。

### 20:46-21:22｜回合触发与战斗切人最终修正
- 将结束回合触发调整为“手动点击时检测全员无牌”，避免误触发。
- 优化战斗切人箭头定位，支持卡组页即时切人刷新。
- 恢复本地多控“整局放弃”能力，并修正左侧头像箭头落点到血条右侧。

## 当前已实现的功能效果（归并视图）

- 可在单机内构建本地多角色（2~4 起步，后扩展并收敛上限）并完成角色控制切换。
- 已打通从大厅/选角到战斗、事件、奖励、下一幕的主要状态流转。
- 多角色下的资源与界面一致性（金币、遗物、药水、奖励、顶部栏）完成多轮修复。
- 切人相关 UI（选角页、头像区、战斗页）已形成稳定可用方案并完成多次定位修正。
- 自动结束回合与看门狗机制落地，降低无牌僵持和流程卡死概率。
- 构建与部署流程已工程化：项目根 `DualRoleAdventure.dll` + `DualRoleAdventure.pck` 作为统一发布/联调产物。
