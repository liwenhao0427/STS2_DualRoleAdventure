# LocalMultiControl 开发任务单

## 目标（阶段 A）

- 在联机入口新增“与自己联机”。
- 首版固定 2 人本地多控（标准局）。
- 战斗内支持 `[` / `]` 切换当前操控角色。
- 奖励/商店/事件等流程尽量复用多人同步器，资源归属到对应角色。

## 已确认约束

- 首版人数固定 2 人。
- 允许重复角色。
- 共享事件采用自动代投。
- 暂不优先支持 Daily / Custom。

## 任务拆解

### T1 - 本地多控会话骨架

- [x] 新增本地会话状态管理（2 人固定槽位）。
- [x] 维护当前操控角色 `CurrentControlledPlayerId`。
- [x] 提供切换接口：`SwitchNextPlayer()` / `SwitchPrevPlayer()`。

### T2 - 联机入口注入

- [x] 在联机 Host 菜单注入“与自己联机”按钮。
- [x] 按钮进入标准局角色选择流程。
- [x] 默认创建 2 人本地队伍（iter-03 接入本地回环后完成）。

### T3 - 本地回环网络适配

- [x] 提供 `INetGameService` 的本地回环实现（仅本 Mod 使用）。
- [x] 支持按当前操控角色发送消息（可指定 senderId）。
- [x] 消息处理保持幂等，避免重复分发。

### T4 - 控制上下文切换

- [x] 切换角色时同步更新 `LocalContext.NetId`。
- [x] 确保手牌、结束回合、目标选择跟随当前角色（当前为最小闭环，持续补充）。
- [x] UI 增加当前操控角色提示。

### T5 - 非战斗流程归属验证

- [x] 奖励选择归属正确（首版通过本地身份同步 + 日志验证）。
- [x] 商店购买/删牌归属正确（首版通过本地身份同步 + 日志验证）。
- [x] 事件选择（含共享事件自动代投）归属正确。

### T6 - 日志与自检

- [x] 关键路径日志前缀统一 `[LocalMultiControl]`。
- [x] 本地构建通过：`dotnet build LocalMultiControl.csproj -c Debug`。
- [x] 格式检查通过：`dotnet format LocalMultiControl.csproj --verify-no-changes`。

## 开发顺序

1. 先做 T1 + T2，打通入口到开局路径。
2. 再做 T3 + T4，打通战斗内切换闭环。
3. 最后做 T5 + T6，补齐流程与验证。

## 迭代与验收规则（新增）

- 每一步都必须产出一个可在游戏中直接验证的小版本（可运行、可操作、可观察日志）。
- 每一步完成后都执行一次完整最小验证：
  - 能进游戏并加载 Mod。
  - 触发该步对应功能路径。
  - 在 `godot.log` 看到对应 `[LocalMultiControl]` 关键日志。
- 每一步都单独提交一次 commit，提交信息需标注迭代编号（如 `iter-01`、`iter-02`）。
- 若某步无法形成可验证闭环，必须先补“临时可验证开关/入口/日志”再进入下一步。

## 小版本计划（可直接验证）

### iter-01（已完成）

- 范围：T1 本地多控会话骨架。
- 可验证点：进入任意 Run，查看会话初始化/清理日志。
- 日志关键字：`本地多控会话已初始化`、`重置本地多控会话`。

### iter-02（已完成）

- 范围：T2 联机入口注入（“与自己联机”）。
- 可验证点：主菜单联机页可见并可点击新入口。
- 日志关键字：`进入与自己联机流程`、`已跳转到角色选择界面`。

### iter-03（已完成）

- 范围：T3 本地回环网络适配基础。
- 可验证点：可进入2人标准局并创建双角色运行态。
- 日志关键字：`本地回环广播消息`、`sender切换`。

### iter-03.1（已完成）

- 范围：iter-03 开局黑屏阻塞修复。
- 可验证点：开局后不再卡在战斗同步等待，可正常进入房间。
- 日志关键字：`本地回环已注入第二玩家同步消息`。

### iter-04（计划）

- 范围：T4 战斗内角色切换最小闭环。
- 可验证点：战斗中 `[` / `]` 可切换操控角色。
- 日志关键字：`切换操控角色(下一位)`、`切换操控角色(上一位)`。

### iter-04.1（已完成）

- 范围：地图路线自动跟投（2人本地模式）。
- 可验证点：地图仅手动投一次即可自动补齐第二票并推进路线。
- 日志关键字：`地图自动跟投`、`地图自动跟投完成`。

### iter-04.2（已完成）

- 范围：切换可见性与本地玩家ID一致性修复。
- 可验证点：切换时出现屏幕提示；不再出现 local player id 与存档玩家ID不一致的验证错误。
- 日志关键字：`检测到切换热键`、`本地双人ID已解析`。

### iter-04.3（已完成）

- 范围：大厅可切换编辑角色 + 关卡切换稳定性修复。
- 可验证点：角色选择界面可切换当前编辑角色；进房不再因战斗同步等待卡死/闪退。
- 日志关键字：`大厅编辑角色切换`、`本地双人模式已禁用战斗同步等待`。

### iter-04.4（已完成）

- 范围：大厅自动全员就绪与切换提示优化。
- 可验证点：点击出发后不再卡“等待其他玩家”；切换提示显示槽位而不是长ID。
- 日志关键字：`本地双人模式自动就绪`。

### iter-04.5（已完成）

- 范围：旧多人存档清理 + 本地回环崩溃日志修复。
- 可验证点：进入“与自己联机”时自动清理旧 `current_run_mp.save`；不再出现 `RunLobby.AbandonRun` 强转报错与远端鼠标空引用刷屏。
- 日志关键字：`已清理历史多人存档`、`本地双人模式接管 RunLobby.AbandonRun`。

### iter-05（已完成）

- 范围：T5 归属与选择链路修复。
- 可验证点：切换角色后，同步器本地玩家身份同步到当前控制角色；共享事件可自动代投；涅奥与后续选择不再因 sender 错位卡死。
- 日志关键字：`控制上下文已更新`、`事件自动代投`、`战斗UI已刷新到当前角色`。

### iter-06（已完成）

- 范围：T6 构建与格式门禁收口。
- 可验证点：本地 `build` 与 `format --verify-no-changes` 通过。
- 日志关键字：`[LocalMultiControl]` 关键链路日志可覆盖入口、开局、切换、地图跟投、清理。

### iter-06.1（已完成）

- 范围：阶段A联调问题修复（角色高亮、战斗能量UI、非共享事件双角色收益、自动换手可操作性、战斗外顶部栏切换）。
- 可验证点：
  - 选角切换后高亮与当前编辑角色一致。
  - 战斗切换后能量/星能与手牌同步变化。
  - 非共享事件选择后另一角色可获得镜像选择结果（首版策略）。
  - 结束回合自动切换后可继续操作另一角色。
  - 战斗外切换后顶部卡组按钮打开的是当前角色卡组。
- 日志关键字：`事件自动代选`、`战斗UI已刷新到当前角色`、`检测到角色 .* 结束回合，自动切换到下一位`。

### iter-06.2（已完成）

- 范围：大厅选角视觉同步与开局卡死修复。
- 可验证点：
  - 切换大厅编辑角色后，角色按钮高亮/方点与当前编辑槽位一致。
  - 点击开始后不再停在大厅，能够进入开局流程。
- 日志关键字：`大厅编辑角色切换`、`本地回环 Lobby 开局流程完成`。

### iter-06.3（已完成）

- 范围：大厅选中残影修复 + 回合切换后操作状态修复。
- 可验证点：
  - 大厅切换角色时，不会残留多个高亮/方点，且与左上角角色一一对应。
  - 一个角色结束回合后自动切换到下一位时，若仍在玩家回合可继续操作。
- 日志关键字：`大厅编辑角色切换`、`战斗UI已刷新到当前角色`。

### iter-06.4（已完成）

- 范围：涅奥复杂选择防卡死 + 结束回合后等待链路修复。
- 可验证点：
  - 非共享事件不再自动代选，避免进入远端选择等待导致卡死。
  - 自动切换仅在仍有本地玩家可行动时触发；两人都结束回合后不再触发回退链路。
  - 两人结束回合后可正常推进敌方回合。
- 日志关键字：`所有角色均已结束回合，跳过自动切换`、`本地双人模式禁用回合回退`。

### iter-06.5（已完成）

- 范围：敌方回合推进补齐 + 非共享事件视图切换。
- 可验证点：
  - 两人都结束回合后，自动补齐第二个 `ReadyToBeginEnemyTurn`，敌方回合可开始。
  - 非共享事件中切换角色后，事件面板切换到对应角色的事件状态/选项。
- 日志关键字：`本地双人模式自动补齐敌方回合就绪`、`非共享事件视图已切换到玩家`。

### iter-06.6（已完成）

- 范围：非共享事件自动切人 + 遗物动画空引用防卡死 + 敌方回合推进兜底。
- 可验证点：
  - 非共享事件中任一角色选项确认后自动切换到另一角色视图。
  - 2号位在涅奥拿遗物类选项不再因遗物栏找不到节点而抛异常卡死。
  - 双方结束回合后自动补齐敌方回合就绪并推进敌方行动。
- 日志关键字：`event-auto-next`、`跳过遗物动画`、`本地双人模式自动补齐敌方回合就绪`。

### iter-06.7（已完成）

- 范围：非共享事件自动切人时机修正 + 敌方回合就绪补齐增强。
- 可验证点：
  - 非共享事件在“当前角色事件流程完成”后再自动切到下一角色，避免奖励归属被切人抢占。
  - 两人结束回合后，`SetReadyToBeginEnemyTurn` 进入时即补齐另一个玩家 ready，确保可推进敌方回合。
- 日志关键字：`记录事件自动切换请求`、`event-auto-next`、`本地双人模式自动补齐敌方回合就绪`。

### iter-06.8（已完成）

- 范围：战斗奖励双角色可领 + 敌方回合推进兜底增强 + 事件自动切人延迟到弹窗结束。
- 可验证点：
  - 战斗奖励会按玩家逐个打开，不再只给一个角色。
  - 双方结束回合后若未推进，自动触发敌方回合兜底推进。
  - 非共享事件的自动切人会等奖励/选牌弹窗关闭后再切换。
- 日志关键字：`打开奖励界面`、`检测到敌方回合未推进，触发本地兜底推进`、`事件流程已完成，等待奖励/选择弹窗关闭后自动切换角色`。

### iter-06.9（已完成）

- 范围：结束回合回退禁用补漏 + 战斗奖励双角色发放 + 药水动画防异常 + 顶部资源切换刷新。
- 可验证点：
  - 双方结束回合后不再出现 Undo 链路干扰，可推进敌方回合。
  - 战斗结算后会按两名角色分别弹奖励界面。
  - 药水奖励在切换视图场景下不再因动画节点缺失导致领取失败。
  - 切换角色后顶部药水/遗物显示可刷新。
- 日志关键字：`忽略结束回合回退点击`、`打开奖励界面`、`跳过药水动画`。

## 当前执行状态

- 状态：进行中
- 当前子任务：iter-07.0 涅奥双人选择链路回归验证

## 已知问题池（待逐项修复）

### P0（阻塞主流程）

- [ ] 涅奥开局仅 1 人可选，第二角色无法完成对应选择流程。
- [ ] 战斗结算奖励仍只出现 1 人份，双角色奖励未稳定发放。
- [ ] 休息区（Rest Site）仅 1 人可操作/结算，未形成双角色闭环。

### P1（高优先可用性）

- [ ] 切换角色后遗物显示会叠加，旧角色遗物节点未正确清空再初始化。
- [ ] 非战斗背包/卡牌相关界面切换角色后显示未稳定刷新（需专项回归）。
- [ ] 药水/遗物动画在部分界面切换时仍存在潜在归属错位风险（持续观察日志）。

### P2（稳定性与一致性）

- [ ] 敌方回合推进链路需继续观察，确保不再受 Undo/ready 时序影响。
- [ ] 事件/奖励/地图/商店等跨阶段切换时，控制权接管/释放幂等性继续回归。

## 下一迭代计划（草案）

### iter-07.0（计划）

- 范围：涅奥双人选择链路专项修复（含开局奖励/选项归属）。
- 可验证点：两名角色均可在涅奥阶段独立完成选择并进入地图。
- 日志关键字：`涅奥选择`、`事件自动切换请求`、`控制上下文已更新`。

### iter-07.0（已实现，待验证）

- 范围：补充非共享事件 Proceed 拦截逻辑，若另一角色事件未完成则强制切到未完成角色，避免涅奥阶段提前离开。
- 可验证点：
  - 涅奥阶段第一名角色完成后点击 Proceed 不会直接离开，而是切到第二名角色。
  - 第二名角色完成后才允许 Proceed 进入地图。
- 日志关键字：`检测到另一名角色尚未完成事件，拦截 Proceed 并切换`、`控制上下文已更新`。

### iter-07.1（计划）

- 范围：遗物栏切换清理与重建，修复叠加显示。
- 可验证点：任意场景切换角色后遗物栏仅显示当前角色遗物，不叠层。
- 日志关键字：`遗物栏刷新`、`战斗UI已刷新到当前角色`。

### iter-07.2（计划）

- 范围：战斗奖励与休息区双角色流程补齐。
- 可验证点：战斗后与休息区均可按两名角色依次完成选择与结算。
- 日志关键字：`打开奖励界面`、`RestSite`、`控制上下文已更新`。
